# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build output
node_modules
dist
dist-ssr

# Environment variables (never commit API keys)
.env
.env.local
.env.*.local

# Claude Code — local session permissions (machine-specific, changes every session)
.claude/settings.local.json

# Claude Code — spec-sync hook state (ephemeral, per-session)
.claude/hooks/.state/

# CLAUDE.md — project-level AI instructions (personal config, not shared)
claude.md
CLAUDE.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Performance documentation
overview-perf-explained.txt

